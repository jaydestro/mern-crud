apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "jayaksdemo-efc2"
    labels:
        app: "jayaksdemo-efc2"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: default-jayaksdemo-efc2.3e0077d599ce474481cd.eastus.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "jayaksdemo-efc2"
                servicePort: 3000