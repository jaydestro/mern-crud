apiVersion: v1
kind: Service
metadata:
    name: "jayaksdemo-efc2"
    labels:
        app: "jayaksdemo-efc2"
spec:
    type: ClusterIP
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "jayaksdemo-efc2"